---
const sectionLinks = [
	{ index: "02", label: "DREAMING", target: "feature-dream" },
	{ index: "03", label: "LOCAL", target: "feature-local" },
	{ index: "04", label: "HARNESSES", target: "feature-harness" },
	{ index: "05", label: "SOURCES", target: "feature-sources" },
];

const harnesses = [
	{
		name: "Claude Code",
		key: "claude",
		kind: "session hooks",
		logo: "/dashboard/logos/claude.svg",
		href: "https://docs.anthropic.com/en/docs/claude-code",
	},
	{
		name: "OpenCode",
		key: "opencode",
		kind: "runtime plugin",
		logo: "/dashboard/logos/opencode.svg",
		href: "https://github.com/sst/opencode",
	},
	{
		name: "OpenClaw",
		key: "openclaw",
		kind: "runtime adapter",
		logo: "/dashboard/logos/openclaw.svg",
		href: "https://github.com/openclaw/openclaw",
	},
	{
		name: "Codex",
		key: "codex",
		kind: "hooks + MCP",
		logo: "/dashboard/logos/openai.svg",
		href: "https://github.com/openai/codex",
	},
	{
		name: "Hermes Agent",
		key: "hermes",
		kind: "memory provider",
		logo: "/dashboard/logos/hermes-agent.svg",
		href: "https://github.com/NousResearch/hermes-agent",
	},
	{
		name: "Gemini CLI",
		key: "gemini",
		kind: "MCP + GEMINI.md",
		logo: "/dashboard/logos/gemini.svg",
		href: "https://github.com/google-gemini/gemini-cli",
	},
	{
		name: "Pi",
		key: "pi",
		kind: "connector",
		logo: "/dashboard/logos/pi.svg",
		href: "https://github.com/Signet-AI/signetai/tree/main/integrations/pi/connector",
	},
	{
		name: "Oh My Pi",
		key: "ohmypi",
		kind: "runtime extension",
		logo: "/dashboard/logos/oh-my-pi.svg",
		href: "https://github.com/Signet-AI/signetai/tree/main/integrations/oh-my-pi/connector",
	},
];
---

<section id="capabilities" class="feature-sections" aria-labelledby="capabilities-heading">
	<div class="container">
		<nav class="feature-nav" aria-label="Capability sections">
			<span class="feature-nav-label">CAPABILITIES</span>
			<span class="feature-nav-rule" aria-hidden="true"></span>
			<span class="feature-nav-range">02 — 05</span>
			<ul>
				{sectionLinks.map((item) => (
					<li><a href={`#${item.target}`}><span>{item.index}</span>{item.label}</a></li>
				))}
			</ul>
		</nav>

		<header class="feature-manifesto reveal">
			<div class="manifesto-index">THE MEMORY LAYER<br />02 — 05</div>
			<div class="manifesto-copy">
				<p class="feature-kicker">PERSISTENT CONTEXT, WITHOUT THE BLACK BOX</p>
				<h2 id="capabilities-heading">Keep the work.<br /><em>Lose the reset.</em></h2>
				<p>
					Signet keeps the useful parts of a working relationship — decisions, preferences,
					people, and evidence — available wherever the next session starts.
				</p>
			</div>
			<div class="manifesto-aside"><span>01</span><p>memory should outlive the interface that created it</p></div>
		</header>

		<div class="memory-layer-bento" aria-label="Memory layer capabilities">
			<article class="memory-layer-card memory-layer-card--signal reveal">
				<div class="memory-card-top"><span>THE MEMORY LAYER</span><span>01 / 04</span></div>
				<div class="memory-signal-copy">
					<p class="memory-card-kicker">ONE WORKSPACE / FOUR KINDS OF CONTEXT</p>
					<h3>Everything useful,<br /><em>still here.</em></h3>
				</div>
				<div class="memory-rack" aria-hidden="true">
					<div class="memory-rack-row"><span class="memory-rack-dot"></span><span>decisions</span><span class="memory-rack-line"></span><span>retained</span></div>
					<div class="memory-rack-row"><span class="memory-rack-dot"></span><span>preferences</span><span class="memory-rack-line"></span><span>portable</span></div>
					<div class="memory-rack-row"><span class="memory-rack-dot"></span><span>people</span><span class="memory-rack-line"></span><span>linked</span></div>
					<div class="memory-rack-row"><span class="memory-rack-dot"></span><span>evidence</span><span class="memory-rack-line"></span><span>inspectable</span></div>
				</div>
				<p class="memory-card-foot">Source-backed memory that stays available across sessions.</p>
			</article>

			<article class="memory-layer-card memory-layer-card--accent reveal">
				<p class="memory-card-kicker">INSPECTABLE BY DEFAULT</p>
				<h3>Readable<br /><em>by design.</em></h3>
				<p>Every claim keeps a path back to its source. Nothing important disappears into a black box.</p>
				<span class="memory-accent-mark" aria-hidden="true"><i></i><i></i><i></i></span>
			</article>

			<article class="memory-layer-card memory-layer-card--small reveal">
				<div class="memory-card-top"><span>02 / 04</span><span>LOCAL</span></div>
				<strong>PORTABLE<br /><em>FILES.</em></strong>
				<p>Back up, inspect, or move the workspace without asking permission.</p>
			</article>

			<article class="memory-layer-card memory-layer-card--wide reveal">
				<div class="memory-card-top"><span>03 / 04</span><span>ONE MEMORY LAYER</span></div>
				<div class="memory-wide-body">
					<div><h4>Decisions, people, preferences, evidence.</h4><p>Keep the useful parts of a working relationship together, wherever the next session starts.</p></div>
					<div class="memory-bars" aria-hidden="true"><span></span><span></span><span></span><span></span><span></span><span></span></div>
				</div>
			</article>
		</div>

		<article id="feature-dream" class="dream-bento" aria-labelledby="dream-heading">
			<header class="dream-bento-head reveal">
				<div class="dream-bento-index"><span class="slice-index">02 / 05</span><span class="slice-kicker">DREAMING</span></div>
				<h3 id="dream-heading">It gets smarter<br /><span>after you leave.</span></h3>
				<p>When a session goes idle, Signet turns the transcript into durable memory, then quietly keeps it current. No save button. No extra prompt.</p>
			</header>
			<div class="dream-bento-grid">
				<article class="dream-bento-card dream-bento-card--core reveal">
					<div class="dream-card-top"><span>SESSION → MEMORY</span><span>01 / 03</span></div>
					<div class="dream-core-intro">
						<p class="dream-card-kicker">WHEN THE WINDOW CLOSES</p>
						<h4>Work ends.<br /><em>Context stays.</em></h4>
						<p>The memory layer picks up where the harness leaves off, then keeps the useful parts ready for next time.</p>
					</div>
					<div class="dream-signal-field" aria-hidden="true">
						<div class="dream-signal-grid"></div>
						<div class="dream-signal-route"><span></span><i></i><b></b><em></em></div>
						<div class="dream-signal-caption"><span>SESSION</span><span>DREAMING</span><span>MEMORY</span></div>
					</div>
				</article>

				<article class="dream-bento-card dream-bento-card--loop reveal">
					<div class="dream-card-top"><span>02 / 03 · DREAMING LOOP</span><span>QUIET WORK</span></div>
					<h4>Three quiet<br /><span>moves.</span></h4>
					<ol class="dream-loop-list">
						<li><span>01</span><div><strong>Distill</strong><p>turn the transcript into a useful shape</p></div></li>
						<li><span>02</span><div><strong>Supersede</strong><p>replace stale claims instead of stacking them</p></div></li>
						<li><span>03</span><div><strong>Retain</strong><p>keep the graph useful between sessions</p></div></li>
					</ol>
				</article>

				<article class="dream-bento-card dream-bento-card--ready reveal">
					<div class="dream-card-top"><span>03 / 03 · NEXT SESSION</span><span>READY</span></div>
					<h4>Ready<br /><em>when you are.</em></h4>
					<div class="dream-ready-state"><span class="dream-ready-dot"></span><strong>SOURCE-BACKED</strong><small>available on return</small></div>
				</article>

				<article class="dream-bento-card dream-bento-card--promise reveal">
					<div class="dream-promise-copy"><span class="dream-card-kicker">THE PROMISE</span><p>No save button.<br /><em>No extra prompt.</em></p></div>
					<div class="dream-promise-rail" aria-hidden="true"><span></span><span></span><span></span><span></span></div>
				</article>
			</div>
		</article>

		<section id="feature-local" class="local-band" aria-labelledby="local-heading">
			<div class="local-band-top">
				<span class="slice-index">03 / 05</span>
				<span class="slice-kicker">LOCAL BY DEFAULT</span>
				<span class="local-band-tag">NO CLOUD ACCOUNT REQUIRED</span>
			</div>
			<div class="local-band-grid">
				<div class="local-band-copy reveal">
					<h3 id="local-heading">Yours to run.<br />Yours to move.</h3>
					<p>One binary, a SQLite workspace, and a daemon on your machine. Your context is portable files — not a subscription-shaped silo.</p>
				</div>
				<div class="local-signal reveal" aria-label="A local Signet workspace stays on your machine">
					<div class="local-signal-top"><span>WORKSPACE / LOCAL</span><span>NO ACCOUNT</span></div>
					<div class="local-signal-route" aria-hidden="true">
						<span>AGENT</span><i></i><span>SIGNET</span><i></i><span>FILES</span>
					</div>
					<div class="local-signal-path">~/.agents/signet</div>
					<p>Memory, sources, and secrets stay with the workspace.</p>
				</div>
				<div class="local-measures reveal">
					<div><span>01</span><strong>ONE BINARY</strong><p>install the full stack in one command</p></div>
					<div><span>02</span><strong>READABLE FILES</strong><p>back up, inspect, or move your workspace</p></div>
					<div><span>03</span><strong>LOCAL RECALL</strong><p>search and ranking stay on-device</p></div>
				</div>
			</div>
		</section>

		<section id="feature-harness" class="harness-slice" aria-labelledby="harness-heading">
			<div class="harness-slice-top">
				<div class="harness-slice-heading reveal">
					<span class="slice-index">04 / 05</span>
					<p class="slice-kicker">PORTABLE CONTEXT</p>
					<h3 id="harness-heading">One context.<br /><span>Every harness.</span></h3>
				</div>
				<div class="harness-slice-copy reveal">
					<p>Signet installs underneath the tools you already use. Switch shells without starting over — the same memory and secrets follow.</p>
					<span class="harness-count">08 CONNECTORS / ONE MEMORY LAYER</span>
				</div>
			</div>
			<div class="harness-ribbon reveal" aria-label="Supported agent harnesses">
				{harnesses.map((harness) => (
					<a class="harness-link" data-logo={harness.key} href={harness.href} target="_blank" rel="noopener noreferrer">
						<span class="harness-mark" aria-hidden="true"><img src={harness.logo} alt="" width="34" height="34" loading="lazy" /></span>
						<span class="harness-link-copy"><strong>{harness.name}</strong><small>{harness.kind}</small></span>
						<span class="harness-link-arrow" aria-hidden="true">↗</span>
					</a>
				))}
			</div>
			<div class="harness-slice-foot"><span></span><p>the harness is where the work happens — memory is what travels</p><a href="https://docs.signetai.sh/harnesses/" target="_blank" rel="noopener noreferrer">read the connector docs ↗</a></div>
		</section>

		<section id="feature-sources" class="sources-slice" aria-labelledby="sources-heading">
			<div class="sources-copy reveal">
				<span class="slice-index">05 / 05</span>
				<p class="slice-kicker">SOURCE-BACKED / TRACEABLE ANSWERS</p>
				<h3 id="sources-heading">See where<br /><span>it came from.</span></h3>
				<p class="slice-body">Signet links every memory to the note, transcript, or file it came from. Open the receipt to inspect the exact source before you trust the answer.</p>
				<a class="text-link" href="https://docs.signetai.sh/memory/" target="_blank" rel="noopener noreferrer">read the provenance guide ↗</a>
			</div>
			<div class="sources-proof-grid reveal" role="group" aria-label="A memory claim linked to a source artifact with a receipt.">
				<article class="sources-proof-claim">
					<div class="sources-proof-top"><span>MEMORY CLAIM / SOURCE ATTACHED</span><span>VALID</span></div>
					<p>“The context from our last session is still here — with the source attached.”</p>
					<div class="sources-proof-meta"><span>source: session-2026-09-09</span><span>claim 01</span><span>2d ago</span></div>
				</article>
				<article class="sources-proof-chain">
					<div class="sources-proof-top"><span>HOW THIS ANSWER WAS BUILT</span><span>SOURCE → CLAIM → RECEIPT</span></div>
					<ol class="source-chain">
						<li><span>01</span><div><strong>Artifact</strong><p>session-2026-09-09.md</p></div><small>ground truth</small></li>
						<li><span>02</span><div><strong>Claim</strong><p>context retained</p></div><small>derived</small></li>
						<li><span>03</span><div><strong>Receipt</strong><p>line 42 · inspectable</p></div><small>valid</small></li>
					</ol>
				</article>
			</div>
			<div class="sources-foot reveal"><span>01</span><span>artifact</span><span>02</span><span>claim</span><span>03</span><span>receipt</span></div>
		</section>
	</div>
</section>

<style>
	.feature-sections {
		border-top: 1px solid var(--color-border);
		padding-block: 0;
	}

	.feature-nav {
		display: grid;
		grid-template-columns: auto minmax(40px, 1fr) auto minmax(0, 2.4fr);
		gap: 14px;
		align-items: center;
		padding: 24px 0;
		border-bottom: 1px solid var(--color-border);
		font-family: var(--font-mono);
		font-size: 0.62rem;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.feature-nav-label { color: var(--color-text-bright); font-weight: 600; }
	.feature-nav-range { color: var(--color-text-muted); }
	.feature-nav-rule { border-top: 1px solid var(--color-border-strong); }
	.feature-nav ul { display: flex; justify-content: space-between; gap: 18px; list-style: none; }
	.feature-nav a { color: var(--color-text-muted); text-decoration: none; transition: color var(--dur) var(--ease); }
	.feature-nav a:hover, .feature-nav a:focus-visible { color: var(--color-text-bright); }
	.feature-nav a span { margin-right: 8px; color: var(--color-accent); }

	.feature-kicker,
	.slice-kicker,
	.slice-index,
	.manifesto-index,
	.manifesto-aside,
	.local-band-tag,
	.harness-count,
	.harness-link-copy small,
	.harness-slice-foot,
	.sources-foot {
		font-family: var(--font-mono);
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	.feature-kicker,
	.slice-kicker { color: var(--color-highlight-text); font-size: 0.64rem; font-weight: 600; }
	.slice-index,
	.manifesto-index,
	.local-band-tag,
	.harness-count,
	.harness-slice-foot,
	.sources-foot { color: var(--color-text-muted); font-size: 0.59rem; }

	.feature-manifesto {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(180px, 220px);
		gap: clamp(28px, 6vw, 96px);
		align-items: end;
		min-height: clamp(420px, 58vh, 620px);
		padding-block: clamp(78px, 10vw, 140px) clamp(68px, 8vw, 116px);
		border-bottom: 1px solid var(--color-border);
	}

	.manifesto-index { grid-column: 1 / -1; align-self: start; line-height: 1.7; }
	.manifesto-copy { grid-column: 1; max-width: 900px; }
	.manifesto-copy h2 {
		margin-top: 22px;
		font-family: var(--font-body);
		font-size: clamp(3.5rem, 8.2vw, 8.6rem);
		font-weight: 500;
		line-height: 0.9;
		letter-spacing: -0.075em;
		text-transform: none;
	}
	.manifesto-copy h2 em { color: var(--color-highlight-text); font-style: normal; }
	.manifesto-copy > p:last-child { max-width: 620px; margin-top: 38px; color: var(--color-text); font-size: clamp(1rem, 1.5vw, 1.18rem); line-height: 1.65; }
	.manifesto-aside { grid-column: 2; align-self: end; display: flex; gap: 12px; line-height: 1.55; }
	.manifesto-aside span { color: var(--color-accent); }
	.manifesto-aside p { max-width: 150px; }

	.memory-layer-bento {
		max-width: 1120px;
		margin: clamp(44px, 7vw, 88px) auto clamp(92px, 12vw, 156px);
		display: grid;
		grid-template-columns: minmax(0, 1.35fr) minmax(260px, 0.65fr);
		grid-template-rows: minmax(380px, auto) minmax(196px, auto);
		gap: 18px;
	}
	.memory-layer-card {
		position: relative;
		min-width: 0;
		overflow: hidden;
		padding: clamp(24px, 3vw, 38px);
		border: 1px solid var(--color-border-strong);
		background: rgba(12, 30, 46, 0.56);
	}
	.memory-card-top {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 16px;
		color: var(--color-text-muted);
		font: 0.58rem var(--font-mono);
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}
	.memory-card-kicker {
		color: var(--color-highlight-text);
		font: 0.62rem var(--font-mono);
		font-weight: 600;
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}
	.memory-layer-card--signal {
		grid-column: 1;
		grid-row: 1;
		min-height: 380px;
		display: flex;
		flex-direction: column;
		background: linear-gradient(140deg, rgba(16, 39, 59, 0.92), rgba(8, 22, 34, 0.82));
	}
	.memory-signal-copy { margin-top: auto; }
	.memory-signal-copy h3,
	.memory-layer-card--accent h3 {
		margin-top: 18px;
		font-family: var(--font-body);
		font-size: clamp(2.5rem, 5vw, 5.4rem);
		font-weight: 500;
		line-height: 0.92;
		letter-spacing: -0.07em;
		text-transform: none;
	}
	.memory-signal-copy h3 em,
	.memory-layer-card--accent h3 em,
	.memory-layer-card--small strong em { color: var(--color-highlight-text); font-style: normal; }
	.memory-rack {
		margin-top: clamp(30px, 4vw, 48px);
		border-top: 1px solid rgba(169, 184, 244, 0.2);
		border-bottom: 1px solid rgba(169, 184, 244, 0.2);
	}
	.memory-rack-row {
		display: grid;
		grid-template-columns: 8px minmax(90px, 0.55fr) minmax(100px, 1fr) auto;
		align-items: center;
		gap: 12px;
		min-height: 36px;
		border-bottom: 1px solid rgba(169, 184, 244, 0.12);
		font: 0.62rem var(--font-mono);
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}
	.memory-rack-row:last-child { border-bottom: 0; }
	.memory-rack-row > span:nth-child(2) { color: var(--color-text); }
	.memory-rack-row > span:last-child { color: var(--color-text-muted); font-size: 0.54rem; }
	.memory-rack-dot { width: 5px; height: 5px; border-radius: 50%; background: var(--color-highlight-text); box-shadow: 0 0 10px rgba(169, 184, 244, 0.65); }
	.memory-rack-line { position: relative; height: 3px; overflow: hidden; background: linear-gradient(90deg, rgba(120, 146, 232, 0.18), rgba(169, 184, 244, 0.7), rgba(120, 146, 232, 0.12)); }
	.memory-rack-line::after { content: ""; position: absolute; inset: 0 auto 0 -26%; width: 26%; background: var(--color-text-bright); box-shadow: 0 0 10px var(--color-highlight-text); animation: memory-scan 3.8s linear infinite; }
	.memory-rack-row:nth-child(2) .memory-rack-line::after { animation-delay: -1.2s; }
	.memory-rack-row:nth-child(3) .memory-rack-line::after { animation-delay: -2.1s; }
	.memory-rack-row:nth-child(4) .memory-rack-line::after { animation-delay: -2.8s; }
	.memory-card-foot { margin-top: 22px; color: var(--color-text-muted); font-size: 0.78rem; line-height: 1.45; }
	.memory-layer-card--accent {
		grid-column: 2;
		grid-row: 1;
		display: flex;
		flex-direction: column;
		min-height: 380px;
		background: var(--color-highlight-text);
		color: #0b1725;
		border-color: rgba(169, 184, 244, 0.9);
	}
	.memory-layer-card--accent .memory-card-kicker { color: #3a5680; }
	.memory-layer-card--accent h3 { color: #0b1725; font-size: clamp(2.4rem, 4.2vw, 4.4rem); }
	.memory-layer-card--accent h3 em { color: #425f90; }
	.memory-layer-card--accent > p:last-of-type { max-width: 280px; margin-top: 28px; color: #31496b; font-size: 0.9rem; line-height: 1.55; }
	.memory-accent-mark { display: flex; align-items: end; gap: 6px; min-height: 74px; margin-top: auto; }
	.memory-accent-mark i { display: block; width: 12px; height: 32px; background: rgba(11, 23, 37, 0.52); }
	.memory-accent-mark i:nth-child(2) { height: 54px; background: rgba(11, 23, 37, 0.68); }
	.memory-accent-mark i:nth-child(3) { height: 40px; background: rgba(11, 23, 37, 0.36); }
	.memory-layer-card--small {
		grid-column: 1;
		grid-row: 2;
		display: grid;
		grid-template-columns: minmax(110px, 0.8fr) minmax(0, 1fr);
		gap: 24px;
		align-items: end;
		background:
			linear-gradient(105deg, rgba(7, 18, 29, .24) 0%, rgba(7, 18, 29, .08) 54%, rgba(7, 18, 29, .02) 100%),
			url("/portable-files-bg.png") center 52% / cover no-repeat;
		border-color: rgba(169, 184, 244, .34);
	}
	.memory-layer-card--small .memory-card-top { grid-column: 1 / -1; align-self: start; }
	.memory-layer-card--small strong { color: var(--color-text-bright); font: 500 clamp(1.5rem, 2.5vw, 2.35rem)/0.9 var(--font-body); letter-spacing: -0.06em; }
	.memory-layer-card--small p { max-width: 270px; color: var(--color-text-muted); font-size: 0.78rem; line-height: 1.45; }
	.memory-layer-card--wide {
		grid-column: 2;
		grid-row: 2;
		background: rgba(18, 37, 54, 0.66);
	}
	.memory-wide-body { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 30px; align-items: end; height: calc(100% - 28px); padding-top: 28px; }
	.memory-wide-body h4 { color: var(--color-text-bright); font: 500 clamp(1.35rem, 2.2vw, 2rem)/1.03 var(--font-body); letter-spacing: -0.05em; text-transform: none; }
	.memory-wide-body p { max-width: 370px; margin-top: 10px; color: var(--color-text-muted); font-size: 0.76rem; line-height: 1.45; }
	.memory-bars { display: flex; align-items: end; gap: 5px; height: 72px; }
	.memory-bars span { display: block; width: 9px; height: 26px; border: 1px solid rgba(169, 184, 244, 0.42); background: rgba(169, 184, 244, 0.16); }
	.memory-bars span:nth-child(2) { height: 44px; }
	.memory-bars span:nth-child(3) { height: 62px; background: rgba(169, 184, 244, 0.42); }
	.memory-bars span:nth-child(4) { height: 36px; }
	.memory-bars span:nth-child(5) { height: 52px; background: rgba(169, 184, 244, 0.28); }
	.memory-bars span:nth-child(6) { height: 30px; }
	@keyframes memory-scan { from { transform: translateX(0); } to { transform: translateX(485%); } }

	.dream-bento {
		padding-block: clamp(100px, 13vw, 180px);
		border-bottom: 1px solid var(--color-border);
	}
	.dream-bento-head { width: min(820px, 100%); margin: 0 auto; text-align: center; }
	.dream-bento-index { display: flex; justify-content: center; align-items: center; gap: 18px; }
	.dream-bento-head h3 { margin-top: 24px; font-family: var(--font-body); font-size: clamp(3.6rem, 6.6vw, 6.8rem); font-weight: 500; line-height: .9; letter-spacing: -.075em; text-transform: none; }
	.dream-bento-head h3 span { color: var(--color-highlight-text); }
	.dream-bento-head > p { max-width: 650px; margin: 32px auto 0; color: var(--color-text); font-size: 1.03rem; line-height: 1.7; }
	.dream-bento-grid { max-width: 1120px; margin: clamp(64px, 8vw, 104px) auto 0; display: grid; grid-template-columns: repeat(12, minmax(0, 1fr)); grid-template-rows: minmax(300px, auto) minmax(224px, auto); gap: 18px; }
	.dream-bento-card { position: relative; min-width: 0; overflow: hidden; padding: clamp(24px, 3vw, 38px); border: 1px solid var(--color-border-strong); background: rgba(12, 30, 46, .56); }
	.dream-card-top { display: flex; align-items: center; justify-content: space-between; gap: 16px; color: var(--color-text-muted); font: .58rem var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
	.dream-card-kicker { color: var(--color-highlight-text); font: .62rem var(--font-mono); font-weight: 600; letter-spacing: .1em; text-transform: uppercase; }
	.dream-bento-card--core { grid-column: 1 / span 6; grid-row: 1 / span 2; min-height: 580px; display: flex; flex-direction: column; background: linear-gradient(140deg, rgba(16, 39, 59, .94), rgba(8, 22, 34, .86)); }
	.dream-core-intro { margin-top: auto; }
	.dream-core-intro h4, .dream-bento-card--loop h4, .dream-bento-card--ready h4 { margin-top: 18px; color: var(--color-text-bright); font: 500 clamp(2.3rem, 4.8vw, 5rem)/.91 var(--font-body); letter-spacing: -.07em; text-transform: none; }
	.dream-core-intro h4 em, .dream-bento-card--ready h4 em { color: var(--color-highlight-text); font-style: normal; }
	.dream-core-intro > p:last-child { max-width: 470px; margin-top: 24px; color: var(--color-text); font-size: .9rem; line-height: 1.55; }
	.dream-signal-field { position: relative; min-height: 176px; margin-top: 38px; overflow: hidden; border-top: 1px solid rgba(169, 184, 244, .2); background: radial-gradient(circle at 52% 54%, rgba(120, 146, 232, .2), transparent 52%); }
	.dream-signal-grid { position: absolute; inset: 20% 4% 10%; background: radial-gradient(circle, rgba(169, 184, 244, .55) 0 1px, transparent 1.5px) 0 0 / 11px 11px; opacity: .16; -webkit-mask-image: linear-gradient(90deg, transparent, #000 15% 84%, transparent); mask-image: linear-gradient(90deg, transparent, #000 15% 84%, transparent); }
	.dream-signal-route { position: absolute; left: 7%; right: 7%; top: 56%; border-top: 1px solid rgba(169, 184, 244, .48); }
	.dream-signal-route span, .dream-signal-route i, .dream-signal-route b { position: absolute; top: -6px; display: block; width: 12px; height: 12px; border: 1px solid var(--color-highlight-text); border-radius: 50%; background: #0b1b27; }
	.dream-signal-route span { left: 0; }
	.dream-signal-route i { left: 48%; box-shadow: 0 0 0 7px rgba(169, 184, 244, .06), 0 0 18px rgba(169, 184, 244, .45); }
	.dream-signal-route b { right: 0; }
	.dream-signal-route em { position: absolute; top: -3px; left: 18%; width: 6px; height: 6px; border-radius: 50%; background: var(--color-text-bright); box-shadow: 0 0 12px var(--color-highlight-text); animation: dream-signal-travel 4.2s ease-in-out infinite; }
	.dream-signal-caption { position: absolute; left: 5%; right: 5%; bottom: 10px; display: flex; justify-content: space-between; color: var(--color-text-muted); font: .55rem var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
	@keyframes dream-signal-travel { 0%, 100% { left: 18%; opacity: .5; } 50% { left: 78%; opacity: 1; } }
	.dream-bento-card--loop { grid-column: 7 / -1; grid-row: 1; min-height: 300px; background: var(--color-highlight-text); color: #0b1725; border-color: rgba(169, 184, 244, .9); }
	.dream-bento-card--loop .dream-card-top { color: #3a5680; }
	.dream-bento-card--loop h4 { color: #0b1725; font-size: clamp(2.1rem, 4vw, 4rem); }
	.dream-bento-card--loop h4 span { color: #425f90; }
	.dream-loop-list { display: grid; gap: 0; margin-top: 26px; list-style: none; }
	.dream-loop-list li { display: grid; grid-template-columns: 28px minmax(0, 1fr); gap: 10px; padding: 11px 0; border-top: 1px solid rgba(11, 23, 37, .2); }
	.dream-loop-list li > span { color: #4b6794; font: .58rem var(--font-mono); }
	.dream-loop-list strong { display: block; color: #0b1725; font: 600 .7rem var(--font-mono); letter-spacing: .05em; text-transform: uppercase; }
	.dream-loop-list p { margin-top: 3px; color: #31496b; font-size: .72rem; line-height: 1.35; }
	.dream-bento-card--ready { grid-column: 7 / span 3; grid-row: 2; display: flex; flex-direction: column; justify-content: space-between; background: rgba(12, 30, 46, .3); }
	.dream-bento-card--ready h4 { font-size: clamp(1.65rem, 3vw, 2.75rem); }
	.dream-ready-state { display: grid; grid-template-columns: 9px minmax(0, 1fr); column-gap: 9px; align-items: center; margin-top: 28px; }
	.dream-ready-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--color-highlight-text); box-shadow: 0 0 12px rgba(169, 184, 244, .8); }
	.dream-ready-state strong { color: var(--color-text-bright); font: .62rem var(--font-mono); letter-spacing: .09em; }
	.dream-ready-state small { grid-column: 2; color: var(--color-text-muted); font: .58rem var(--font-mono); margin-top: 3px; }
	.dream-bento-card--promise { grid-column: 10 / -1; grid-row: 2; display: flex; align-items: center; justify-content: space-between; gap: 24px; background: rgba(18, 37, 54, .68); }
	.dream-bento-card--promise::before { content: ""; position: absolute; inset: 0; pointer-events: none; opacity: .55; background: repeating-linear-gradient(135deg, transparent 0 12px, rgba(169, 184, 244, .16) 12px 13px); }
	.dream-bento-card--promise > * { position: relative; z-index: 1; }
	.dream-promise-copy p { margin-top: 15px; color: var(--color-text-bright); font: 500 clamp(1.25rem, 2.5vw, 2.2rem)/1.02 var(--font-body); letter-spacing: -.05em; }
	.dream-promise-copy p em { color: var(--color-highlight-text); font-style: normal; }
	.dream-promise-rail { display: flex; align-items: end; gap: 5px; height: 56px; }
	.dream-promise-rail span { display: block; width: 8px; height: 20px; background: rgba(169, 184, 244, .24); border: 1px solid rgba(169, 184, 244, .45); }
	.dream-promise-rail span:nth-child(2) { height: 38px; }
	.dream-promise-rail span:nth-child(3) { height: 52px; background: rgba(169, 184, 244, .42); }
	.dream-promise-rail span:nth-child(4) { height: 32px; }

	.local-band-copy h3,
	.harness-slice-heading h3,
	.sources-copy h3 { margin-top: 16px; font-family: var(--font-body); font-size: clamp(2.6rem, 5vw, 5.1rem); font-weight: 500; line-height: 0.98; letter-spacing: -0.06em; text-transform: none; }
	.slice-body { max-width: 530px; margin-top: 28px; color: var(--color-text); font-size: 1.03rem; line-height: 1.7; }

	.local-band { position: relative; overflow: hidden; margin-inline: calc(clamp(20px, 4vw, var(--space-xl)) * -1); padding: clamp(84px, 10vw, 132px) clamp(20px, 4vw, var(--space-xl)) clamp(0px, 2vw, 24px); background: #c4d2e7; color: #0b1725; }
	.local-band::before { content: ""; position: absolute; inset: 0; pointer-events: none; opacity: .34; background: radial-gradient(circle at 50% 26%, rgba(255,255,255,.5), transparent 42%); }
	.local-band-top { position: relative; z-index: 1; display: flex; align-items: center; gap: 18px; color: #3d5778; font: .6rem var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
	.local-band-top .slice-index { color: #36547e; }
	.local-band-tag { margin-left: auto; color: #476487; }
	.local-band-grid { position: relative; z-index: 1; display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(320px, .95fr); gap: clamp(48px, 8vw, 120px); align-items: stretch; margin-top: clamp(56px, 7vw, 84px); }
	.local-band-copy { max-width: 860px; margin: 0; align-self: center; text-align: left; }
	.local-band-copy h3 { color: #0a1625; font-size: clamp(3.4rem, 6.5vw, 7.4rem); line-height: .88; }
	.local-band-copy p { max-width: 560px; margin: 28px 0 0; color: #304662; font-size: 1.02rem; line-height: 1.65; }
	.local-signal { display: flex; min-height: 260px; flex-direction: column; justify-content: space-between; padding: clamp(22px, 3vw, 32px); border: 1px solid rgba(20, 43, 69, .42); background: linear-gradient(145deg, rgba(11, 29, 47, .98), rgba(16, 43, 69, .92)); color: #dfe9f8; box-shadow: 16px 18px 0 rgba(47, 74, 108, .14); }
	.local-signal-top { display: flex; justify-content: space-between; gap: 16px; color: #91a9c7; font: .58rem var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
	.local-signal-route { display: grid; grid-template-columns: auto minmax(28px, 1fr) auto minmax(28px, 1fr) auto; align-items: center; gap: 10px; margin: 24px 0; color: #dfe9f8; font: .64rem var(--font-mono); letter-spacing: .1em; }
	.local-signal-route i { position: relative; height: 1px; background: rgba(169, 184, 244, .48); }
	.local-signal-route i::after { content: ""; position: absolute; top: -2px; right: 0; width: 5px; height: 5px; border-radius: 50%; background: #a9b8f4; box-shadow: 0 0 12px rgba(169, 184, 244, .75); }
	.local-signal-path { width: fit-content; padding: 9px 11px; border: 1px solid rgba(169, 184, 244, .26); color: #a9b8f4; font: .7rem var(--font-mono); letter-spacing: .04em; }
	.local-signal p { max-width: 260px; margin-top: 20px; color: #adc0d8; font-size: .82rem; line-height: 1.45; }
	.local-measures { grid-column: 1 / -1; display: grid; grid-template-columns: repeat(3, minmax(0,1fr)); border-top: 1px solid rgba(20,43,69,.3); }
	.local-measures > div { display: flex; min-height: 166px; flex-direction: column; gap: 13px; padding: 22px 24px 28px; border-right: 1px solid rgba(20,43,69,.22); border-bottom: 1px solid rgba(20,43,69,.22); }
	.local-measures > div:last-child { border-right: 0; }
	.local-measures span { color: #3c5e8e; font: .62rem var(--font-mono); }
	.local-measures strong { color: #0b1725; font: .7rem var(--font-mono); letter-spacing: .08em; }
	.local-measures p { max-width: 220px; margin-top: auto; color: #304662; font-size: .82rem; line-height: 1.4; }

	.harness-slice { padding-block: clamp(110px, 14vw, 188px); border-bottom: 1px solid var(--color-border); }
	.harness-slice-top { display: grid; grid-template-columns: 1fr; gap: 28px; justify-items: center; text-align: center; }
	.harness-slice-heading h3 { font-size: clamp(3.5rem, 7vw, 7.8rem); }
	.harness-slice-heading h3 span { color: var(--color-highlight-text); }
	.harness-slice-copy { max-width: 560px; padding-bottom: 0; }
	.harness-slice-copy p { color: var(--color-text); font-size: 1rem; line-height: 1.65; }
	.harness-count { display: block; margin-top: 28px; color: var(--color-text-muted); }
	.harness-ribbon { display: grid; grid-template-columns: repeat(8, minmax(0,1fr)); margin-top: 74px; border-top: 1px solid var(--color-border-strong); border-bottom: 1px solid var(--color-border-strong); }
	.harness-link { position: relative; display: flex; min-height: 146px; flex-direction: column; align-items: center; justify-content: center; gap: 14px; padding: 20px 10px; border-right: 1px solid var(--color-border-strong); color: var(--color-text); text-decoration: none; transition: background-color var(--dur) var(--ease), color var(--dur) var(--ease); }
	.harness-link:last-child { border-right: 0; }
	.harness-link:hover, .harness-link:focus-visible { background: rgba(17,38,58,.72); color: var(--color-text-bright); }
	.harness-mark { display: grid; place-items: center; width: 38px; height: 38px; }
	.harness-mark img { display: block; width: 34px; height: 34px; object-fit: contain; filter: saturate(.72) brightness(1.18); opacity: .9; transition: filter var(--dur) var(--ease), opacity var(--dur) var(--ease), transform var(--dur) var(--ease); }
	.harness-link:hover .harness-mark img, .harness-link:focus-visible .harness-mark img { filter: saturate(1) brightness(1.08); opacity: 1; transform: scale(1.08); }
	.harness-link[data-logo="hermes"] .harness-mark img { filter: invert(1) saturate(.2) brightness(1.2); }
	.harness-link[data-logo="ohmypi"] .harness-mark img { filter: invert(1) saturate(.2) brightness(1.2); }
	.harness-link-copy { display: flex; flex-direction: column; align-items: center; gap: 5px; min-width: 0; text-align: center; }
	.harness-link-copy strong { color: var(--color-text-bright); font-size: .78rem; font-weight: 500; line-height: 1.2; }
	.harness-link-copy small { color: var(--color-text-muted); font-size: .52rem; letter-spacing: .04em; white-space: nowrap; }
	.harness-link-arrow { position: absolute; right: 10px; top: 10px; color: var(--color-text-muted); font-size: .72rem; opacity: .7; }
	.harness-slice-foot { display: flex; align-items: center; justify-content: center; gap: 10px; margin-top: 24px; text-align: center; }
	.harness-slice-foot span { width: 6px; height: 6px; border-radius: 50%; background: var(--color-highlight-text); box-shadow: 0 0 10px rgba(169,184,244,.55); }
	.harness-slice-foot a { margin-left: 16px; color: var(--color-highlight-text); text-decoration: none; }
	.harness-slice-foot a:hover { color: var(--color-text-bright); }

	.sources-slice { display: grid; grid-template-columns: 1fr; gap: clamp(56px, 7vw, 86px); align-items: center; padding-block: clamp(110px, 14vw, 188px); }
	.sources-copy { max-width: 820px; margin: 0; text-align: left; }
	.sources-copy h3 { font-size: clamp(3.5rem, 7vw, 7.5rem); }
	.sources-copy h3 span { color: var(--color-highlight-text); }
	.sources-copy .slice-body { margin-inline: 0; }
	.text-link { display: inline-block; margin-top: 30px; color: var(--color-highlight-text); font: .68rem var(--font-mono); letter-spacing: .08em; text-decoration: none; text-transform: uppercase; }
	.text-link:hover { color: var(--color-text-bright); }
	.sources-proof-grid { display: grid; grid-template-columns: minmax(0, .82fr) minmax(0, 1.18fr); gap: 18px; align-items: stretch; }
	.sources-proof-claim, .sources-proof-chain { min-width: 0; padding: clamp(24px, 4vw, 42px); border: 1px solid var(--color-border-strong); }
	.sources-proof-claim { display: flex; flex-direction: column; justify-content: space-between; min-height: 330px; background: var(--color-highlight-text); border-color: rgba(169, 184, 244, .88); color: #0b1725; }
	.sources-proof-chain { min-height: 330px; background: rgba(12, 30, 46, .56); }
	.sources-proof-top { display: flex; align-items: center; justify-content: space-between; gap: 14px; color: var(--color-text-muted); font: .58rem var(--font-mono); letter-spacing: .1em; text-transform: uppercase; }
	.sources-proof-claim .sources-proof-top { color: #3a5680; }
	.sources-proof-claim > p { max-width: 620px; margin-top: 50px; color: #0b1725; font: 500 clamp(1.65rem, 3.1vw, 3rem)/1.08 var(--font-body); letter-spacing: -.06em; }
	.sources-proof-meta { display: flex; align-items: center; flex-wrap: wrap; gap: 10px 20px; margin-top: auto; padding-top: 28px; color: #3a5680; font: .58rem var(--font-mono); letter-spacing: .03em; text-transform: none; }
	.source-chain { display: grid; gap: 0; margin-top: 28px; list-style: none; }
	.source-chain li { display: grid; grid-template-columns: 30px minmax(0, 1fr) auto; gap: 14px; align-items: center; min-height: 70px; border-top: 1px solid var(--color-border); }
	.source-chain li > span { color: var(--color-accent); font: .58rem var(--font-mono); }
	.source-chain strong { color: var(--color-text-bright); font: 500 .78rem var(--font-mono); letter-spacing: .07em; text-transform: uppercase; }
	.source-chain p { margin-top: 4px; color: var(--color-text-muted); font-size: .76rem; line-height: 1.35; }
	.source-chain small { color: var(--color-highlight-text); font: .56rem var(--font-mono); letter-spacing: .05em; text-transform: uppercase; }
	.sources-foot { grid-column: auto; display: flex; align-items: center; gap: 14px; padding-top: 22px; border-top: 1px solid var(--color-border); }
	.sources-foot span:nth-child(odd) { color: var(--color-accent); }
	.sources-foot span:nth-child(even) { margin-right: 20px; }

	@media (max-width: 980px) {
		.feature-nav { grid-template-columns: auto auto; justify-content: space-between; }
		.feature-nav-rule { display: none; }
		.feature-nav ul { grid-column: 1 / -1; }
		.feature-manifesto { grid-template-columns: 1fr; gap: 28px; min-height: 0; }
		.manifesto-index, .manifesto-copy, .manifesto-aside { grid-column: auto; }
		.manifesto-index { order: 0; }
		.manifesto-aside { order: 2; }
		.memory-layer-bento { grid-template-columns: minmax(0, 1.1fr) minmax(220px, .9fr); grid-template-rows: minmax(340px, auto) minmax(188px, auto); }
		.dream-bento-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); grid-template-rows: auto; gap: 12px; }
		.dream-bento-card--core { grid-column: 1 / -1; grid-row: auto; min-height: 470px; }
		.dream-bento-card--loop { grid-column: 1; grid-row: auto; min-height: 300px; }
		.dream-bento-card--ready { grid-column: 2; grid-row: auto; }
		.dream-bento-card--promise { grid-column: 1 / -1; grid-row: auto; }
		.local-band-grid, .harness-slice-top, .sources-slice { grid-template-columns: 1fr; gap: 52px; }
		.local-band { margin-inline: calc(clamp(20px, 4vw, var(--space-xl)) * -1); }
		.local-measures { grid-column: auto; }
		.harness-ribbon { grid-template-columns: repeat(4, minmax(0,1fr)); }
		.harness-link { min-height: 118px; gap: 10px; padding: 16px 10px; }
		.harness-mark { width: 32px; height: 32px; }
		.harness-mark img { width: 30px; height: 30px; }
		.harness-link-copy strong { font-size: .7rem; }
		.harness-link-copy small { font-size: .48rem; }
		.sources-proof-grid { grid-template-columns: 1fr; gap: 12px; }
		.sources-foot { grid-column: auto; }
	}

	@media (max-width: 600px) {
		.feature-nav ul { display: grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 12px; }
		.feature-nav a { font-size: .57rem; }
		.manifesto-copy h2 { font-size: clamp(3.2rem, 16vw, 5.6rem); }
		.memory-layer-bento { grid-template-columns: 1fr; grid-template-rows: auto; gap: 10px; margin-top: 40px; margin-bottom: 88px; }
		.memory-layer-card--signal,
		.memory-layer-card--accent,
		.memory-layer-card--small,
		.memory-layer-card--wide { grid-column: auto; grid-row: auto; }
		.memory-layer-card--signal { min-height: 350px; }
		.memory-layer-card--accent { min-height: 300px; }
		.memory-layer-card--small { min-height: 200px; }
		.memory-layer-card--small { background-image: linear-gradient(105deg, rgba(7, 18, 29, .3) 0%, rgba(7, 18, 29, .1) 64%, rgba(7, 18, 29, .03) 100%), url("/portable-files-bg.png"); background-position: center 52%; }
		.memory-layer-card--wide { min-height: 212px; }
		.memory-signal-copy h3 { font-size: clamp(2.4rem, 12vw, 4.2rem); }
		.memory-rack-row { grid-template-columns: 8px minmax(74px, .62fr) minmax(74px, 1fr) auto; gap: 8px; font-size: .55rem; }
		.memory-rack-row > span:last-child { font-size: .48rem; }
		.memory-wide-body { grid-template-columns: 1fr; height: auto; gap: 18px; }
		.memory-bars { justify-content: flex-start; height: 58px; }
		.dream-bento { padding-block: 88px; }
		.dream-bento-head h3 { font-size: clamp(3.2rem, 15vw, 5.4rem); }
		.dream-bento-head > p { margin-top: 24px; }
		.dream-bento-grid { grid-template-columns: 1fr; gap: 10px; margin-top: 52px; }
		.dream-bento-card--core,
		.dream-bento-card--loop,
		.dream-bento-card--ready,
		.dream-bento-card--promise { grid-column: auto; grid-row: auto; }
		.dream-bento-card--core { min-height: 450px; }
		.dream-bento-card--loop { min-height: 0; }
		.dream-signal-field { min-height: 154px; margin-top: 28px; }
		.dream-core-intro h4 { font-size: clamp(2.2rem, 11vw, 4rem); }
		.dream-loop-list p { font-size: .68rem; }
		.dream-bento-card--ready { min-height: 190px; }
		.dream-bento-card--promise { min-height: 150px; }
		.local-band-top { flex-wrap: wrap; }
		.local-band-tag { width: 100%; margin-left: 0; }
		.local-band-copy { text-align: left; }
		.local-band-copy p { margin-left: 0; margin-right: 0; }
		.local-signal { min-height: 220px; box-shadow: 10px 12px 0 rgba(47, 74, 108, .12); }
		.local-signal-route { gap: 7px; font-size: .56rem; }
		.local-measures { grid-template-columns: 1fr; }
		.local-measures > div { min-height: 132px; border-right: 0; gap: 8px; }
		.harness-ribbon { grid-template-columns: repeat(2, minmax(0, 1fr)); }
		.harness-ribbon { gap: 1px; border: 1px solid var(--color-border-strong); background: var(--color-border-strong); }
		.harness-link, .harness-link:last-child { border: 0; background: rgba(7, 18, 29, .72); }
		.harness-link { min-height: 104px; align-items: flex-start; justify-content: flex-start; gap: 10px; padding: 14px 12px; }
		.harness-mark { width: 30px; height: 30px; }
		.harness-mark img { width: 28px; height: 28px; }
		.harness-link-copy { width: 100%; align-items: flex-start; padding-right: 4px; text-align: left; }
		.harness-link-copy strong { font-size: .68rem; }
		.harness-link-copy small { white-space: normal; font-size: .47rem; line-height: 1.25; }
		.harness-link-arrow { top: 8px; right: 8px; }
		.harness-slice-foot { align-items: flex-start; flex-wrap: wrap; }
		.harness-slice-foot a { width: 100%; margin-left: 16px; }
		.sources-proof-claim, .sources-proof-chain { min-height: 250px; padding: 22px; }
		.sources-proof-claim > p { margin-top: 32px; font-size: clamp(1.5rem, 8vw, 2.5rem); }
		.source-chain li { grid-template-columns: 26px minmax(0, 1fr) auto; gap: 10px; min-height: 64px; }
		.source-chain strong { font-size: .66rem; }
		.source-chain p { font-size: .68rem; }
		.sources-foot { flex-wrap: wrap; gap: 10px; }
		.sources-foot span:nth-child(even) { margin-right: 8px; }
	}
</style>
